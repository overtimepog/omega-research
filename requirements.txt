# Core dependencies for EvolveAgent
# Python 3.10+ recommended

# Configuration and environment
python-dotenv>=1.0.0
pyyaml>=6.0

# LLM API clients (OpenRouter-compatible)
openai>=1.30.0

# Data processing (minimal dependencies)
numpy>=1.24.0

torch
torchtune
torchao
torchvision

# Development dependencies (optional)
# pytest>=7.4.0  # For running tests
# black>=23.0.0  # Code formatting
# flake8>=6.0.0  # Linting

# Notes:
# - All LLM operations use OpenRouter API (no local models required)
# - vLLM, transformers, datasets, and pandas are NO LONGER NEEDED
# - Reward model is API-based only (model_type: "api")
